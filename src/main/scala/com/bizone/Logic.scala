package com.bizone

class Logic {
  var a: Int = 1
  var b: Int = 9
  val joinResult: Int = a + b
  def total(a: Int, b: Int): Int = {
    return a + b
  }

  def show(a: Int): Unit = {
    println(a)
  }

  def div(a: Int, b: Int): Float = {
    return a / b
  }
}
